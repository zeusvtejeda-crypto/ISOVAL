import { Dashboard } from '@/components/dashboard/Dashboard';

/** Inicio: panel con el progreso, la sesión de hoy y los accesos a todos los modos. */
export default function HomePage() {
  return <Dashboard />;
}
