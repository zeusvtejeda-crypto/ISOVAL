// Pendiente de implementar (vista 'register').
import { emptyState } from '../lib/ui.js';
export default {
  title: 'En construcción',
  render(el) { el.innerHTML = String(emptyState({ icon: 'sparkles', title: 'Sección en construcción', text: 'Esta pantalla estará lista muy pronto.' })); }
};
