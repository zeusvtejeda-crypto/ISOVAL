// Pendiente (agente E): aviso por correo al dueño cuando llega una reserva en línea.
// Nunca lanza: devuelve { ok, error? }.
export async function sendBookingEmail(env, shop, appointment) { return { ok: false, error: 'not_configured' }; }
